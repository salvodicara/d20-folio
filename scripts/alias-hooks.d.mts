// Type declarations for the plain-ESM admin-script module hooks (alias-hooks.mjs),
// so TypeScript consumers (the unit test) get types for its pure export.
export function expandImportMetaGlob(source: string, url: string): string;
