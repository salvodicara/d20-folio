---
"d20-folio": patch
---

refactor(share): collapse the anon /view chrome to ONE sign-in button and remove the post-view marketing card (owner gate 2026-07-31). Auth is Google-only and sign-in IS sign-up (the first OAuth creates the account), so the "Sign in" + "Create your character" pair were two doors to one flow — collapsed to a single button, desktop AND mobile, reusing the app's own `auth.signIn` label ("Sign in with Google" / "Accedi con Google", the LoginPage's) so the header and the auth page name it the same. The button keeps the topbar-native scale (text-sm `.btn`, not the tiny 10px `.btn.sm`) and the ≥44px mobile touch floor; the header bar stays byte-identical to the signed-in one except this one swapped button. The inline post-view conversion card ("Like this hero? Create your own on d20 Folio." + the "Built with d20 Folio" wordmark loop) is DELETED — off-tone for the app and redundant with the header sign-in button — along with its component, its five i18n strings (both locales), its CSS, and its tests. The header sign-in button is now the SOLE conversion path; the `/view` body is just the sheet. Net deletion; PROGRESS.md records the "post-view signup CTA" candidate as declined-as-built (not shipped). The eager entry ceiling stays 63 KB (budget unchanged).
