---
"d20-folio": minor
---

Surgical visual rollback to the v0.22.0 baseline: the post-v0.22.0 chrome-reset identity
(plate/veil material, corner knots, hairline grammar, the MARK, removed borders/ornaments,
the compendium treatment) is rejected and reverted — `index.css`, `folio.css`, and the
ornament-bearing components are the v0.22.0 vocabulary again (diamonds, crest watermark,
gilt glow, framed key chips, the commit-ceremony bloom). Every post-v0.22.0 feature keeps
its chrome through the delimited feature-layer appendix in `folio.css`: the monster
statblock plaque, the header-as-disclosure toggle, the realm scenes + backdrop crossfade,
and the a11y/i18n fixes. Look-pinning guard tests are restored to their v0.22.0 versions;
the chrome-system guards that existed only to pin the rejected identity are removed.
