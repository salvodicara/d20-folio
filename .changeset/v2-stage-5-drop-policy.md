---
---

The map's drop policy (`planDrop`: a dropped token becomes a budgeted `move`, a `position` placement, or a refusal, by who drags it, whose turn it is and the automation level) and the viewer projection (`mapView`: hidden tokens, tokens under fog and HP numbers concealed by presenter).
