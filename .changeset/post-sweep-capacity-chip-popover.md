---
"d20-folio": patch
---

fix(inventory): the carrying-capacity and attunement chips explain themselves in the app's folio popover instead of a native tooltip — the push/drag/lift number is now reachable on touch, and a screen reader announces the chip's numbers, not just its label. The read-only chip on the DM and admin sheet views drops its hover-only tooltip, which only restated the label beside it.
