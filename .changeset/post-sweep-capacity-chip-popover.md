---
"d20-folio": patch
---

fix(inventory): the carrying-capacity and attunement chips explain themselves in the app's folio popover instead of a native tooltip — the push/drag/lift number is now reachable on touch.
