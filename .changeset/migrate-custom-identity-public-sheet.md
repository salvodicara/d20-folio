---
---

Migrate the anonymous share projection (`characters/{id}/public/sheet`) together with its parent in the custom-identity one-off, so a shared character's public link survives the apply, and harden the migration kit: corpus-level verification of the projection, reserved library ids across kinds, malformed-container issues, hashed failure reporting, and an emulator-backed test of the guarded apply path.
