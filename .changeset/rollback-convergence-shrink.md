---
"d20-folio": patch
---

Visual-rollback convergence shrink: the feature-layer appendix drops its dead `.rule-above`
rule and the shadowed `.on-art-title` ground (the restored on-art-scope machinery already
covers its one consumer), merges the byte-identical `.mon-line`/`.mon-entry` declarations,
drops a reduced-motion rule the global kill already enforces, and inlines the CR seal's
single-use engraved-numeral ink.
