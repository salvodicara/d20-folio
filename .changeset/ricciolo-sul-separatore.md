---
"d20-folio": patch
---

The divider motif now FUSES with its separator (owner, gate-2 iteration 3): the engraved
center-loop's internal rule is measured at 83.1% of the artwork's height, the ornament pins
to the strip's top, and the flanking hairline sits exactly on that baseline in the SAME ink
per theme (bright gold in dark, deep gold in light via a re-inked variant) — the loop rises
FROM the line instead of floating over it.
