---
---

Pay a levelled cast from a pool the caster actually has: the dispatch builders choose the slot level and pool from the acting entity's resources, so a Warlock's Pact slot is spent instead of a standard slot it does not own.
