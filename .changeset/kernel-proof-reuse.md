---
"d20-folio": patch
---

The causal kernel stops re-proving what its own completed proofs already guarantee stable: kernel-born frozen causal states, structure-parsed worlds, conformed execution frames and exact-schema outputs now conform by identity (possession proofs double as completed proofs), and canonical JSON/fingerprints of deeply frozen trees are cached on the frozen object. Deterministic results are unchanged — hostile or unregistered values still take the full fail-closed proof — and the four slowest kernel scenarios run ~10× faster, now pinned by deterministic proof-work ceilings (`readMechanicsKernelProofStats`) as a performance regression guard.
