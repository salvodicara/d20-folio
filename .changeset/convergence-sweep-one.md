---
"d20-folio": patch
---

Convergence sweep: the engine dispatch honours every legacy boundary (plain-slot options only, shared per-turn slot claim mirrored on commit, pool/metamagic/concentration-swap flows stay legacy), the root-pulse trigger joins the evidence census, PI-safe docs, minted concentration refs, deduplicated and dash-free i18n copy, and the pure-module guard passes with the pulse lister moved into the store layer.
