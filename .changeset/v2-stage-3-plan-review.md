---
"d20-folio": patch
---

Review pass on the stage 3 plan: fix a missing `end-turn` in Sara's replay, a Fireball test that
caught its own caster, a wrong HP expectation and two dead tests, and a `null as never` hack;
couple an HP override above zero to revival; correct the sphere-distance note; narrow the
admin-account claim in the program ledger to what the access matrix actually grants.
