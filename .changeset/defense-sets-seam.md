---
---

Move the set-override seam (`applySetOverride`, `deriveDefenseKind`) out of the presenter layer into engine-core, so the combat projection can read the same effective defenses the rail renders.
