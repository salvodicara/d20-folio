---
"d20-folio": patch
---

Fix the pact-slot world seed: the canonical material holds the pact pool as a bare count cell, so pact casters derive their mechanics world again instead of silently degrading every engine surface to the legacy path.
