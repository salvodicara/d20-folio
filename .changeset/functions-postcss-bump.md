---
"d20-folio": patch
---

Security: the `functions/` lockfile re-resolves its dev-tooling `postcss` transitive to 8.5.25
(GHSA-r28c-9q8g-f849 patched); the remaining react-router RSC-mode alerts stay triaged
non-exploitable for this client-side SPA pending the 8.x major bump.
