---
"d20-folio": patch
---

fix(a11y): the light ink ramps separate against the WHOLE prose vocabulary, not just their own family. Two tokens came out of the AA re-tune closer to a neighbour than they went in. Light `--dmg-radiant-ink` had been separated from lightning by HALVING its chroma at its own lightness (0.088 → 0.043) — at 13px on the carved plaque that read as plain bold body text, and it closed to 0.025 ΔE of bludgeoning; it now goes DEEPER instead (`#4c3e00`, L 0.461 → 0.368, chroma held at 0.075), so it stays gold with lightning the brighter yellow above it and its nearest neighbour anywhere in the prose vocabulary moves 0.019 → 0.041 ΔE (AA 4.628 → 5.557). Light `--cond-deafened-ink` had been darkened by lightness alone and sat 0.020 ΔE from the untouched invisible, the only re-tuned condition that lost separation; it takes the same chroma step blinded and stunned took (`#4a4965`, 0.030 ΔE, AA 4.562, hue held). The guard is hardened to match: the derived plaque ground is pinned to `.beast-ref` having exactly one base rule (a later `[data-theme]` override would otherwise leave it certifying a ground the browser no longer paints), the per-theme derivation is hoisted out of the assertion bodies, and the assertion titles now name the grounds they actually cover.
