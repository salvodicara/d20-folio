---
"d20-folio": patch
---

Convergence trim on the admin unit: the lazy-index effect keys on a boolean
flip instead of every keystroke (the in-flight ref and cancellation ceremony
die), and the search module one-field types inline.
