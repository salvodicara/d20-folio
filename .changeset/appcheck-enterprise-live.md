---
"d20-folio": patch
---

App Check goes live in monitor mode: the client switches to
ReCaptchaEnterpriseProvider (the headlessly-provisionable key kind; token TTL
24h keeps the Enterprise free tier roomy), the rollout runbook and env typings record steps 1–3 as done, and safe-01's update branch learns gcloud's --add-threshold-rule
spelling so re-arms apply the alert steps instead of dying.
