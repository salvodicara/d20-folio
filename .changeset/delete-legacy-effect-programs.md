---
"d20-folio": minor
---

Delete the legacy effect-program generation. The authored `effectProgram` data field (public and pack), its interpreter and the whole planning/command/atomic/lifecycle execution stack, the outer action-command kernel, and the unmounted program-review UI are removed — the canonical MechanicsProgram runtime is the one executable truth. Every ex-carrier still transcribes and dispatches engine (guard-pinned); stored remnants of the deleted runtime are ignored at every read boundary and shed by the next write. The persistent standing-effect ledger stays live with its program-owned payload kinds pruned.
