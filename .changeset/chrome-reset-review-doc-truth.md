---
"d20-folio": patch
---

docs(identity): make the chrome reset's own claims true — L2 restated as what the code implements, and three claims that had drifted. `DESIGN.md` stated L2 two contradictory ways: §4 and §5 both said "wherever a mark appears the rule terminates, the mark occupies the interval, and the rule resumes", while the mark's own entry says the cartouche's rail "passes through unbroken" — and the render agrees, the border runs straight under the leaves. L2 is now "an ornament is the line's OWN LOCAL FORM — never a second rail beside it", which is what the corner terminal and the cartouche actually do. `DESIGN.md` §4 also listed a portrait well among the gems that carry "No border, no lip" while three recipes shipped one or both: `.cmp-frontis-seal` gives up its border AND the banned two-line lip (it is the codex seal at masthead scale, so it wears the codex seal's law) and `.ch-portrait` gives up its doubled outer ring and its cream lip; the sentence is scoped to the two cases where what sits inside the shape is not the gem's own fill — the portrait well, which clips a photograph, and the owner-picked champlevé coin, whose reeded rim is the strike itself. The phase-5 claim "every geometry-changing state is deleted" is corrected in both the changeset and `PROGRESS.md`: it holds for pointer, focus and selection states, while disclosure states still resize the content they REVEAL (the guard names those four bodies as exemptions). The phase-7 budget's "+680 B across three phases" is four (4–7). And an orphaned comment for a deleted light-theme `.fchip` rule, left dangling over `.fchip[aria-pressed="true"] .fc-count`, is gone.
