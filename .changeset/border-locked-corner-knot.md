---
"d20-folio": patch
---

fix(identity): reduce the Compass-Web to the border-locked corner knot under the one-line law (owner ruling — "smaller but beautiful, aligned to the borders"): the ornament contributes no run lines (inner-rail wedge, whisper web, and floating crescent removed — nothing floats), the three framed hero registers go square so the knot seats on a true crossing of the host's own 1px border, the rivet mass diamond is a real path again (bare-text SVG was silently dropped), and the seat divider's baked occlusion plate shrinks to hug the fleur with an in-SVG blurred edge so no hard plate rim shows. The final knot is an owner pick among three drawn variants; the blade-and-rivet essence ships as the interim.
