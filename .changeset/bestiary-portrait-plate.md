---
"d20-folio": patch
---

Bestiary statblock portrait relaid as a real framed PLATE (the D&D Beyond
statblock-with-art grammar): the data spine stays left and the portrait fills the top
region's right column, stretched to the stat region's exact height — one solid
illustrated block with zero dead space, replacing the small floating square. Narrow
containers get the monster-card grammar instead (a centred 4:5 plate above the stats).
The no-art fallback is struck as an illuminated plate initial inside a fine gilt fillet,
so an empty plate reads as a prepared bookplate; the hover camera veil stays the only
affordance. The portrait panel's frame shape now belongs to the caller (the square
library tile is unchanged), and a `d20-dev-monster-art` dev-bypass seed shows the
uploaded-art state offline.
