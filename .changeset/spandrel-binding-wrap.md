---
"d20-folio": patch
---

The spandrel corner now WRAPS the hero frame like a bookbinding fitting (owner pick +
ruling): the host's rounded corner squares off beneath the fitting (the 8px radius made the
border curve out from under it and read as a second line), and pixel-row measurement proves
the fitting's outer rule sits EXACTLY on the host border (row 88 = row 88, full coverage)
with the inner rule at +2px — the deliberate double-line of a real binding corner.
