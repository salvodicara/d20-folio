---
"d20-folio": patch
---

fix(a11y): the rules-text colour grammar survives the bestiary statblock, and its Advantage ink clears WCAG-AA there. `.mon-entry strong` (0,1,1) out-specified the grammar's single-class rules (0,1,0), so every monster entry repainted Advantage / Disadvantage / dice / DCs gilt instead of their semantic inks — three of the grammar's five arms were silently dead on the app's most prose-dense surface (only `.rt-dmg`/`.rt-cond` survived, on inline styles). The gilt lead is a classless `<strong>`, so the rule is now `.mon-entry strong:not([class])`. With the grammar painting again, `.rt-adv` measured 4.156:1 on the carved `--bg-recessed` plaque: it rides `--rt-adv-ink`, a prose-deep variant of the success stop (light `--verdigris-900`, 7.484:1), so the shared `--verdigris-700` palette stop — HP-bar gradient math, badges, borders, `--at-action` — is untouched. Guarded twice: the token pairing in `verdict-ink-contrast.test.ts` (now widened to the plaque) and the composited truth in a new `statblock-ink-contrast.spec.ts`, which measures every ink in real Chromium. The a11y surface leaf moves from `skeleton` (no "Advantage", no "Disadvantage", no condition name — structurally blind to three inks) to `mimic`, the one SRD statblock that fires every arm at once.
