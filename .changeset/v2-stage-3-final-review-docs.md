---
"d20-folio": patch
---

Reconcile the stage-3 record with the branch review: the held-window ruling is reversed (at
`log-only` nothing about a declaration commits), the fix wave's five new rulings and the Adapter-row
correction are recorded, the deferred-minor list separates what was closed from what is still open,
overrides emitting no `CombatEvent` and the `intent.ts` split join the open seams, the replays'
scope is stated plainly, and the authoring spec's §6 Costs and Inputs rows are read off the shipped
unions instead of the spec's own sketch.
