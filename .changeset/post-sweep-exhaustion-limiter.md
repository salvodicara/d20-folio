---
"d20-folio": patch
---

fix(combat): the turn-limiter banner now states the REAL Exhaustion penalties (−2 × level to d20 Tests, −5 ft × level Speed) instead of a hardcoded −2, resolved from the one pair of engine formulas every other surface reads.
