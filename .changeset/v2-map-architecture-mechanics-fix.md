---
"d20-folio": patch
---

Fix ARCHITECTURE.md and MECHANICS.md to name the real `planDrop`/`payCosts` seams instead of invented ones, state the character-creation revision contract, attribute `unavailable: true` to the `featureDestinations` derivation rather than `primaryDestinations`, render the shared `negate`/`manual-table` switch arm faithfully, and cite the writeback module's item-8/P11b fate correctly.
