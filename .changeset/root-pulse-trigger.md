---
"d20-folio": patch
---

Recurring phases advance through the new possession-safe root-pulse trigger: the root's possessor declares each pulse (single-use via advance CAS on execution + event id), and the coordinator admits non-create roots by pushing their frame before review — Moonbeam's pulse now runs end-to-end with register-scaled dice.
