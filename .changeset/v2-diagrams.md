---
"d20-folio": patch
---

Add the five archify diagram sources and their exported SVGs under `docs/diagrams/`: the system architecture, the encounter data flow, one attack with a reaction window, the encounter lifecycle, and character creation and persistence. Each source validates at the showcase profile with all nine artifact checks and no errors or warnings, the architecture diagram verifies its cited files against a pinned revision, and the delivered HTML viewers stay gitignored.
