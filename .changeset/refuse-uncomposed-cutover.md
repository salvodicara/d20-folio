---
---

The legacy parent cutover refuses to plan unless the private content pack actually composed, in every mode: an SRD-only process would see a pack-only spell reference as unknown and silently rewrite a held concentration to a custom name. A per-family guard behind that assertion refuses any stored concentration reference that does not survive canonicalization unchanged, and a character-codec refusal is now reported by its stable code alone, never by the codec path (which can name a stored map key).
