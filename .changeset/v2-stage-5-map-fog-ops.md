---
---

The folded encounter state carries the map: a background reference with its grid, and rectangle fog with one representation (covered except revealed), set by the `map` and `fog` table ops; `Entity.reveal.token` marks a hidden token.
