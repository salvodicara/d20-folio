---
"d20-folio": patch
---

The three ricalco corner frames regenerate VECTOR-EXACT (owner: "fai le cose bene"): arm
centerlines re-derived from the path data itself (the raster estimates were off by up to 43
units), a standalone calibration harness proves line continuity across the arm→border joint
by peak-profile measurement, the gilt-frame host border fuses to the ornament's exact ink
per theme, and the spandrel candidate reseats with its OUTER rule on the border and the
corner knot capping the vertex — a binding fitting, never a floating inset.
