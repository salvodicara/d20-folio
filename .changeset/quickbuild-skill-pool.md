---
"d20-folio": patch
---

One derivation for "which class skills can still be picked" (the class pool minus the ones the
background already grants), shared by the background re-seed and the randomizer instead of living
in both.
