---
"d20-folio": minor
---

Every engine commit now carries the product's native reversal affordance: casts, feature actions, weapon attacks and conjured consumes register the same "X used + Undo" toast grammar the legacy loop owns, whose undo drives the exact journal reverse (slots, pools, HP, concentration and engine conditions restore in one motion) and rolls the legacy economy mirror back with it, with redo riding the reducer's native redo transition and a crossed one-way solo boundary failing closed. The engine concentration swap collapses to its one-action kernel form: confirming a replacement commits old-end plus new-cast as ONE causal journal action (nothing ends at confirm; backing out keeps the old spell), the replacement toast is the undo affordance, and its undo restores the old spell whole. The damage-reaction pick's undo entry is now turn-scoped like every reaction commit.
