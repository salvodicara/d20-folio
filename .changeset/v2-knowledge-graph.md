---
"d20-folio": patch
---

Commit the graphify knowledge graph of the public v2 source (`graphify-out/graph.json`, ~22.8 MB, 31,536 nodes / 42,162 clustered edges across 3,235 communities, plus `GRAPH_REPORT.md`), kept current on every commit by a `.githooks/pre-commit` refresh step scoped to staged `src`/`tests`/`functions`/`scripts` changes. Also add the v2 knowledge-base reset plan (`docs/superpowers/plans/2026-09-09-v2-knowledge-base-reset.md`).
