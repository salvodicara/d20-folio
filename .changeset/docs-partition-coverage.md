---
"d20-folio": patch
---

Partition the tracking docs for the public snapshot: `docs/AUTOMATION_COVERAGE.md` now carries only the SRD corpus (12 classes + their SRD subclasses, SRD feats/species/backgrounds/items/spells) plus a generic content-pack pointer; every pack-entity row and pack-specific audit note moves verbatim to the private `content-pack/docs/AUTOMATION_COVERAGE.md`.
