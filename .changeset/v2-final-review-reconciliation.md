---
"d20-folio": patch
---

Reconcile the four steering statements the final whole-branch review found contradicted by the current facts: `PRODUCT.md` §Steering dates how the "agreed at 100 % from images" method applies on `v2` under the 8 September standing delegation, `PROGRESS.md` assigns real-data migration to P30 (with P11b for the personal-data cutover) instead of P29, `docs/program/NEXT.md` and `docs/PROGRAM_STATUS.md` describe the knowledge-base reset as in flight until the integration commit records its SHA, and `docs/program/PROGRAM.md` plus `NEXT.md` record that the E01–E22 catalogue and the nine player-freedom scenarios must be imported into the repository (rule 36) as a P14a entry condition. Also note in `docs/program/reference/README.md` that the manifest's `status`/`ownerApproval` fields are the lab's freeze-time state superseded by the 2026-09-06 owner approval, and replace the absolute personal paths in three P-documents with `~/Workspace/Codex/...`.
