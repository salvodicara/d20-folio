---
---

Reduce firestore.rules to identity, membership, ownership and shape: every predicate that read a game field is gone, the campaign model is a closed key set, and the admin holds owner-level rights on every user path.
