---
---

Close stage 0 of the stage-1 program on `v2`: P1 data safety merged from `main`, six-fixture byte-identity and a zero-loss production export dry-run recorded, the dry-run command added to the release runbook, the read seams reconciled in the schema document, and the next-session handoff written.
