---
"d20-folio": minor
---

Grow the kernel's standing-fact vocabulary with the recipient-buff kinds and make spell transcription recipient-aware. `zero-hp-floor` wires the kernel's existing remain-at-one policy to a standing-driven selector: engine damage that would drop a warded creature to 0 leaves it at 1 and consumes the whole granting spell atomically (Death Ward, single-use, exact across multi-hit actions, undo restores the ward). `max-hp-delta` carries Aid's cast-level-resolved maximum raise and the paired heal lands into the raised headroom in the same cast; the sheet's effective max HP reads the exact world amount and never double-counts. `damage-transfer` records Warding Bond's payer while the ward's expressible half — resistance to all damage — halves engine damage through the standing damage-defense profile; the cross-creature mirror stays an honest table boundary. `recipient: "selected"` buffs now bind their standings to the selected target entities (gated on the landed hit or failed save exactly like conditions), Heroism arms a per-turn Temp-HP pulse priced by the frozen spellcasting modifier, and self-owned floors and deltas project into the one grants union for the manual damage path.
