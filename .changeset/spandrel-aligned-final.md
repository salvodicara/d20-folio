---
"d20-folio": patch
---

The spandrel corner fitting aligns geometrically (owner directive): the vertex knot
subpath is removed from the CC0 artwork, the fitting mounts as a mask whose outer-rule
OUTER EDGES sit at the tile origin — pixel-verified coincident with the host border on
both axes (top y88=y88, left x96=x96, inner rule +3px) — painted with the app's standard
border ink mix (no border-colour override), the hero-frame corner squared beneath it as a
geometric necessity.
