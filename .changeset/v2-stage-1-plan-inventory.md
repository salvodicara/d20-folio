---
"d20-folio": patch
---

Record the module fates of the architecture reset in the stage-1 program plan, route `CLAUDE.md`'s direction block to the steering and the stage plan, and run the SRD-only CI gate on pushes to `v2`.
