---
---

Reconcile the map into the owner documents: the design spec (§2.1 `map` on the folded state, §2.2 `reveal.token`, §3.1 the `map`/`fog` ops and the direct-patch override paths, §5.1 and §5.4 the Storage path and rule), the architecture map (the map adapter and Storage seam), the test portfolio (counts, the property test, the new replay) and the stage-1 plan (item 5 status).
