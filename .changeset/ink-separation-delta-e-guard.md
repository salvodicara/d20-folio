---
"d20-folio": patch
---

fix(a11y): the ink vocabulary's separation floor is a NUMBER the suite enforces, and light `--cond-paralyzed-ink` clears it. There was no ΔE assertion anywhere in the suite, so every separation claim on this branch was checked by eye — and the AA re-tune shipped its own worst collision: darkening `--dmg-lightning-ink` (#6e5a06 → #5b4900) to clear the carved plaque landed it 0.0079 ΔE from the untouched `--cond-paralyzed-ink`, a NEW global minimum of the 31-token light prose vocabulary, 1.7× tighter than the worst pair the work inherited (nineteen of the thirty-one tokens ended up closer to a neighbour than they started). `tests/unit/verdict-ink-contrast.test.ts` now runs an OKLab ΔE matrix over every unordered pair of that vocabulary in BOTH themes — floors 0.0132 (light, petrified↔restrained) and 0.0105 (dark, piercing↔blinded), the tightest legitimate pair each, with one allowlisted twin (`--dmg-poison-ink` ↔ `--cond-poisoned-ink`, whose BASE tokens are the same hex in both themes) whose exemption is itself asserted. Lightning does not move — the thirteen damage inks are a closed cross-theme colour code that also paints the `.mon-dmg` ledger and the defence chips, and the only separation left at its lightness rotates it into the ramp's own olive band. The condition ink moves instead, inside the warm gold→amber band its base occupies: `#5e4806` → `#5f3a00` (L 0.414 → 0.384, chroma held 0.082 → 0.083, hue 88° → 70°) — 0.0435 ΔE from lightning, 0.0380 from its nearest neighbour anywhere, AA 4.612 → 5.304 on the plaque, chip graphic untouched.
