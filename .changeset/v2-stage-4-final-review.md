---
---

Close the stage-4 whole-branch review: cap the stored encounter log at the codec's real node budget, bound the compaction grace window with the caller's clock, sort the two derived lists compaction canonicalises, pin the encounter shape fence and the self-join banner, and guard the adapters' import boundary.
