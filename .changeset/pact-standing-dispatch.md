---
"d20-folio": patch
---

Close the last spell-dispatch gaps before the legacy effect-program deletion: pact-slot casts run through the engine (the cast modal offers the pact slot with its level, the kernel pays from the world's pact cell, and the commit mirrors the legacy pact usage counter exactly, undo included); caster-owned standing casts (Hex's curse included) dispatch engine, with the world's active-key standing lighting the same "+1d6 vs cursed target" chips on weapon and spell-attack rows that a legacy activation lights — every while-active gating read now goes through the one chips-plus-standings union; maintainer rows and use-applies stay legacy with their model boundaries documented at the exclusion sites; and a guard test pins that every public spell still carrying a legacy effectProgram transcribes and gates engine.
