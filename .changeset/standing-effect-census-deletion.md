---
"d20-folio": minor
---

Execute deletion-map L2's first half on the persistent standing-effect system: the campaign
effectOps ledger stays as the chartered smallest cross-document seam (its multiplayer behavior has
no canonical carrier under the split-owner topology), while the dead limbs are deleted end to end:
the set-active compare-and-swap op kind and algebra, the never-written local CombatState.effectOps
mirror ledger with replaceCombatEffectOps and its codec gates (stored fields now ignored
fail-safe), the never-produced authoredLifetime field, and markedTargetForActor.
