---
"d20-folio": patch
---

fix(a11y): close the ink guards' remaining holes, and say what axe actually does. Four fixes and one disclosure. (1) `--text-special` — the grammar's `.rt-value` arm (dice, save DCs, distances) — was pinned on surface-2/3 only while it also paints the carved statblock plaque; it measures 6.463:1 there, a latent pass that was going uncovered, so the plaque joins its grounds and the describe's "every ground it inks" claim now holds for all five arms. (2) The derived plaque ground was pinned against `.beast-ref` alone, but the element is `class="beast-ref mon-ref"` — a `.mon-ref { background }` (equal specificity, later in the file) would have repainted it while every assertion kept certifying the old colour; `.mon-ref` is now pinned to one base rule declaring no background, mutation-proven. (3) DESIGN.md's "Self-enforcing gates" told the reader to re-run the a11y surface gate after any light-token change — exactly the vacuous coverage this work disproved. It now states the fact plainly and ONCE: axe's `color-contrast` rule is inert on this app APP-WIDE, not on the bestiary specifically (axe-core 4.11.4, both themes: monster entry 121 incomplete / 0 violations, spell entry 84 / 0, settings 22 light and 19 dark / 0), and points at the two gates that do the job. (4) Owner-visible, previously unmentioned: `.mon-entry strong` also set `font-weight: 700`, so narrowing it to `:not([class])` returns `.rt-dmg` / `.rt-cond` / `.rt-value` / `.rt-adv` / `.rt-dis` to the grammar's own 600 on 300+ monster pages — grammar-correct ("all at 600, never the shouted UA 700") but a visible change; before/after crops are in the bestiary-ink scratchpad.
