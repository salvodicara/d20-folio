---
"d20-folio": patch
---

Protruding index tabs, one anatomy app-wide: the character sheet's tab strip
and the compendium type ribbon now share a single construction — opaque book
tabs floating above the page like a notebook's index, the page border passing
in front of the resting tabs' feet, and the active tab fused into the page as
one piece (calibrated page-tone fill, one continuous border stroke, concave
junction fillets, no halo ring). The sheet's center composes the tome's
bound-leaf material as the character's page, clipped tabs dissolve via a mask
instead of a hard cut, and the redundant inner hairline frame is gone.
