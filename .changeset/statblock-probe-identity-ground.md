---
"d20-folio": patch
---

fix(a11y): the statblock ink probe asserts IDENTITY and a REAL ground, not just a ratio. Its docblock claimed "a specificity regression is caught the moment it lands" — mutation-proven false: re-injecting `.mon-entry strong { color: var(--accent-text) }` left the spec at ZERO failures, because gilt measures 6.875:1 on the carved plaque. Comfortably AA, comfortably the wrong colour. The spec now resolves `--rt-adv-ink` / `--semantic-danger` / `--text-special` off the live document and asserts each token-driven arm's computed colour EQUALS its token, plus that no arm (inline-styled ones included) is painted the gilt lead's `--accent-text`; the same mutation now fails both themes, naming every arm. The ancestor walk is hardened the same way: it accepted any background not ending in `,0)`, so `.beast-ref { background: rgba(205,187,142,.35) }` passed with the ratio unchanged, and an element with no painted ancestor fell through to `""` → black, a ground the plaque never renders and every dark ink clears. Alpha is now parsed and both cases FAIL LOUDLY naming the node — the docblock's "this IS the pixel behind the glyphs" is enforced rather than asserted. All three mutations re-run and proven.
