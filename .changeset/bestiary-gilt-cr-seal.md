---
"d20-folio": patch
---

Make the compendium Monsters CR seal theme-invariant gilt jewelry (owner-directed 2026-07-24). The leading challenge-rating seal now renders as the same light-gold gem with a crisp near-black engraved numeral in BOTH themes, instead of re-resolving to a dark bronze-on-bronze gem in the light theme. Pinned at the recipe level to fixed gilt tokens (`.lvl-seal.cr-seal` → `--gold-leaf-500` gem + the new theme-independent `--gilt-ink`), flowing to every strike of the seal (list rows + the entry masthead); the on-art ink-contrast battery locks the pair AA-legible and byte-identical across themes.
