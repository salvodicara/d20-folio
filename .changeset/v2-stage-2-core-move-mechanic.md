---
"d20-folio": patch
---

Add the universal `core:move` mechanic to the prototype catalogue and classify the `move` step
as automated coverage, closing the exhaustiveness gap the step's addition opened. The `move` step
handler now has a real test suite proving budget, first-placement, and derived-relation behavior.
