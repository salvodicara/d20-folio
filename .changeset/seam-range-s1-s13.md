---
"d20-folio": patch
---

docs: correct the S-seam range across the doc set. The `## The closing seams` section in `AUTOMATION_BACKLOG.md` grew to include the S11–S13 Tier-3 primitives but its header/intro (and the `CLAUDE.md` + `PROGRESS.md` current-state summaries) still read `S1–S10` / `S1–S11`. Now uniformly **S1–S13**, and the section header reflects it is effectively CLOSED rather than an open frontier.
