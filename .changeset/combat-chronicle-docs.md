---
"d20-folio": patch
---

Docs: record the Combat Chronicle event seam (the campaign-encounter events-as-data
feed, its emit points, the never-guess attribution / never-auto miss rules, and the
End → one-chapter Chronicle append) in `docs/ARCHITECTURE.md`, and mark it shipped in
`PROGRESS.md`.
