---
"d20-folio": minor
---

Connect guided creation and explicit import comparison to retained drafts, guarded atomic saves and source-aware character reads in English and Italian.

Restore the approved text-only shell navigation and mint table marker across desktop and phone.

Fix saved catalogue consultation to show localized included-source names and real release versions, preserve authenticated class choice labels through nested declarations, and resolve canonical training names without missing translation keys. Preserve custom names and frozen source data. Reopen saved import comparison using the character identity while retaining exact source data.

Register acquisition metadata in the exact automation corpus; retain long-rest cantrip replacement as an explicit bilingual manual boundary. Use canonical selection controls and shared bilingual labels without changing frozen catalogue snapshots.

Keep internal custom-source IDs out of roster summaries and localize equipment-backed tool proficiencies in saved inspection without adding subscriptions or rewriting stored sources.
