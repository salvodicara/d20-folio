---
"d20-folio": patch
---

refactor(identity): the chrome reset's over-engineering sweep — dead flexibility and duplication out. `--state-metal-disabled` had zero consumers and is DELETED rather than wired: disabled is a rung of the state ladder but it is not a metal — it is the honest desaturate-and-flatten recipe on `:disabled` (`.btn:disabled`), so the `DESIGN.md` rung now names the recipe that ships instead of a phantom token, and the guard stops asserting it. `.rule-below` had zero call sites (every separator in the app is "above the group that follows") and goes with its guard entry and its doc mentions; it is four lines to re-add the day markup needs it. `--mark-inset` was a one-value variable read twice in one rule — inlined. `.tr-row:last-child` restated the whole spine gradient to drop one hairline layer; it zeroes that layer's `background-size` instead, so the spine has one definition. The theme-override sweep's `u.replace(/\\\\/g, "\\")` was a no-op on a source-literal pattern. The rail's seat assertion folds into the QUIET tier table (metal is a per-row flag) instead of a bespoke expect beside it. And the action log's attribute-less wrapper `<div>` is gone — its two children are the `<section>`'s. CSS 74,766 → 74,601 B gzipped (−165 B).
