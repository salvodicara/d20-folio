---
"d20-folio": patch
---

chore(dev): one dev scenario per attack-scope defect, and PROGRESS records the wave

Splits the PS-J dev sheet in two so each defect can be looked at in isolation:
`scn-psj-hunter-17` carries Precise Hunter's per-target Advantage alone (the Defensive Tactics
bundle deliberately unpicked — with both clauses on one sheet the two BUGS cancelled each other and
hid the symptom), and `scn-psj-hunter-7` carries Escape the Horde alone. `PROGRESS.md` records the
shipped wave.
