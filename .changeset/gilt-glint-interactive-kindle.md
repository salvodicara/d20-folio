---
"d20-folio": patch
---

The interactive layer reaches BG3 touch fidelity (wave 2): the struck-gold CTAs (primary buttons, End Turn) play a one-shot gilt glint — a quiet specular sweep — on hover, reduced-motion-safe and GPU-only; picker/browse row hover — including the upcast cast-level picker rows — now kindles toward candle-gold instead of a neutral gray fill; and the light theme's compendium tab hover, previously imperceptible on the ivory band, gets its own warm strike.
