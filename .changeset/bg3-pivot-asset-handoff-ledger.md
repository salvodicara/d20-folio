---
"d20-folio": patch
---

Recorded the BG3-pivot asset-integration handoff and ran a ledger truth-sweep in `PROGRESS.md`
(golden rule 16). Added the precise PROMPT_15–25 integration checklist for a future session (drop-in
token swaps 15–17, new per-realm scene wiring 18–23, ornament-mask A/B 24–25, acceptance/budget/
verification bars), reconciled the stale "UI-test-corpus noise audit" follow-up to SHIPPED (the
`test-audit-*` series; `_identity-shots`/`_polish-shots` kept separate by design), and clarified the
"next waves" note so the only remaining pivot work reads as asset integration. Docs only.
