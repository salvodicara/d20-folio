---
---

The minimum map's surface (proposal, behind the screenshot gate): `MapCanvas` — an SVG ground with the background image, the square grid, rectangle fog, tokens bound to entity ids, drag with a Foundry-style ruler toned by the movement budget, the DM's hidden tokens and player view — its pure geometry, the `map` i18n shard, and the DEV-only `/_map` harness on a fixture encounter.
