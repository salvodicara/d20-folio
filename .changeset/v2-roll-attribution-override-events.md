---
---

Bind a per-target save roll to the target it was rolled for, and give a DM's HP override to zero the same 0-HP tail a killing hit has (an `hp-zero` event and ended concentration, no `damage-taken`).
