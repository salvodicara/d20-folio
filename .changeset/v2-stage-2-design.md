---
"d20-folio": patch
---

Design stage 2 on `v2` (positions and areas in the aggregate): grid-cell positions, the four-band
range ladder kept over the UI spec's unratified five-band proposal, derived-vs-declared as
log-order precedence instead of a provenance field, the `move` step's answer-carried destination,
and the five-shape area membership function — addendum to the total-combat-automation design.
