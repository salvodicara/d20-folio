---
---

Rules lane: two clients appending to the encounter in the same round-trip and one client's burst of ten appends all land, and both clients fold the same state (the same-millisecond stamps order by uid).
