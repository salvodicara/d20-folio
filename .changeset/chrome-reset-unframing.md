---
"d20-folio": patch
---

feat(identity): the chrome reset, phase 4 — a frame means container or interactive, and nothing else is framed. Every read-only facet (chips, tags, verdicts, seals, die sizes, raw scores, keywords, stat readouts, the keycap) is type in an alignment column; every list row is frameless at rest with a hairline-bounded wash band when selected; the grouping rail gives up its metal, which buys back the nesting level its interactive tiles need. Framed nesting is 2 on every surface (was 3), the visible framed-box count falls 114 → 18 on the compendium and 261 → 83 on the cockpit, and the budget is now pinned on the rendered page by `tests/e2e/chrome-census.spec.ts`.
