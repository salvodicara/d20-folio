---
---

Record the four `v2` gate wall-clock times measured after the stage-3 final fix wave: `just ci`
4 min 36 s, `pnpm test:rules` 15.1 s, `vite build` + `pnpm test:budget` 5 s, `just ci-srd-only`
2 min 19 s — 7 min 15 s combined, all green.
