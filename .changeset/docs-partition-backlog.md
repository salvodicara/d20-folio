---
"d20-folio": patch
---

Automation-backlog partition: the shipped/open items whose subject is a pack entity (setting species, pack subclasses, pack spells/items) move verbatim to the private `content-pack/docs/AUTOMATION_BACKLOG.md`; pack-entity names and live-fixture identifiers in the remaining generic engine entries are generalized. The public doc keeps the doctrine, taxonomy, seam contract, and every engine item.
