---
---

The encounter log is genuinely append-only — the stored entries must remain a prefix of the written ones — and the rules suite pins the admin delete, the member create denial and the limit of admin-supreme on a published character.
