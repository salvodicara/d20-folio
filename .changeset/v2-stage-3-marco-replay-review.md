---
"d20-folio": patch
---

Marco's golden replay now places him at (0,0) so the move is a budgeted 10-of-30-foot step rather than an unpositioned first placement, and asserts `movementUsed` accordingly.
