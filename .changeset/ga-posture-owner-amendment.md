---
"d20-folio": patch
---

docs(positioning): GA is the ratified destination — owner amendment 2026-07-31

Docs-only capture wave (golden rule 4). The owner ratified the trajectory's destination: at some
point the SRD-clean public build goes generally available and competes with D&D Beyond head-on,
with "objectively better" (correctness, craft, trust, price) as the acceptance bar, the content
moat neutralized by the private pack (personal, forever-private) and the homebrew ladder (the
public answer), and an absolute premium/zero-defect mandate ("we can't afford any bug or weird
UI/UX" — no bug or weird UI/UX can be afforded).

- `docs/POSITIONING.md` — "Option A" becomes the CURRENT PHASE of the ratified GA trajectory; the
  monetization boundary stands unchanged; stale behind-list lines fixed (homebrew library rung (a)
  SHIPPED 2026-07-30, quickbuild parity SHIPPED 2026-07-30); the Verified line carries the
  2026-07-31 posture amendment.
- `PROGRESS.md` — the dated owner amendment lands in the DDB-parity epic charter, and the
  "parked backups/observability/legal" phrasing becomes the named **pre-GA checklist** (backups ·
  observability · legal pages · auth breadth beyond Google · the react-router advisory triage) —
  parked now, blocking GA later. A second owner amendment (2026-07-31) refines the share-links
  charter bullet to the settled scope: **characters only** (Web Share API native sheet + copy-link
  fallback, no viewer account, one flag / one rules line / zero auth changes); campaigns
  deliberately have NO share model (industry standard — a table shares via each player's own
  character; an opt-in campaign surface is a someday path only on real demand).
- `docs/GOLDEN_RULES.md` — rule 27 gains the owner's one-sentence 2026-07-31 reinforcement (the
  bar covers premium craft and UX strangeness, not only functional defects).
- `CLAUDE.md` — the forward-frontier sentence reconciled (pre-GA checklist named; quickbuild +
  homebrew-library shipped; live head = share links).
