---
"d20-folio": patch
---

Steering-doc publishability pass: the admin uid leaves `CLAUDE.md` (read it from the Firestore console), the fixture-name list becomes a generic `content-pack/fixtures/team/` pointer, the wikidot retrieval methodology moves to the new pack-side `content-pack/docs/SOURCING.md` (the public docs keep only the source citation), example owner emails in the Cloud-Functions runbook become placeholders, live-user names leave the schema doc, and the `.gitignore` scrape comments are reworded neutrally (plus the stale fixture-dump path fixed to its pack home).
