---
"d20-folio": patch
---

docs(identity): reconcile `DESIGN.md`, `PROGRESS.md` and the section-header guard with what the chrome reset actually shipped. The guard probed `sec-diamond`, a class the reset deleted, so it could no longer fire — re-anchored on the rubric's own `sec-head`. `DESIGN.md` documented the corner mark as shipping while the ornament guard pins its absence (its spec moves to the Phase 6 row that owns the redraw, and the ornament budget is stated as zero), still carried "The Focus Wash" four lines above "No Emission" (deleted; the ring statement folds into No Emission), and prescribed three things §4 bans — an `--elev-resting` tile on cards, dialogs in the `--elev-floating` overlay row, and a diamond-rubric dialog head that suppresses a focus wash. `PROGRESS.md`'s phase-2 row named an edge assembly (`moat → lip → groove → rim → lip`) that the phase's own law forbids.
