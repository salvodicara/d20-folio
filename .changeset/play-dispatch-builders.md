---
---

Add the play surface's dispatch builders: `planIntent` lists what a tile must roll (or the rejection the fold would record, before a die is spent), `rollsFor` builds the roll actions through the dice seam, and `intentBody` answers each input with its roll.
