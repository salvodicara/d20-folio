---
"d20-folio": patch
---

Content-pack partition review pass 1: scrub the remaining Product-Identity vocabulary from public engine/UI comments (species-revelation / maneuver-subclass phrasing), widen the PI-denylist guard to sweep ALL of `src/**` (code, comments, values), derive the pack i18n loader's kind map from the exported public `SRD_FILE` (drift pair killed, cycle-safe lazy inverse), make the `uiOverlay` merge fail loud on a missing chrome group, and retarget the last inert `entertainer` references in public tests/comments to the SRD Criminal.
