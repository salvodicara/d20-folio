---
"d20-folio": patch
---

chore(campaign): encounter-picker convergence — correct the picker changeset's corpus count to 330 SRD monsters (331 composed) and make the "added to the encounter" toast plural-aware (`encounterAddedToast_one`/`_other`, EN + IT) so multi-add reads `aggiunti` in Italian.
