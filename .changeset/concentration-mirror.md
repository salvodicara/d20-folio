---
"d20-folio": patch
---

The rollout bridge mirrors engine-held concentration onto the legacy session field — engine transitions only, so a legacy-held concentration is never clobbered and an engine release clears only what the engine set.
