---
---

Close a checkpoint round-trip coverage gap, reject `__proto__`/`constructor`/`prototype` keys instead of silently dropping them, cap the unknown-key clone's recursion depth, and document the encounter codec's log-length ceiling.
