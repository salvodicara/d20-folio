---
"d20-folio": patch
---

Compile authored entity, inventory and end-program lifecycle steps: closed capability-snapshot blueprints materialize summons and conjured items exactly, terminal end-program requests latch at the frame pop, and availability/inventory transitions run through the kernel.
