---
---

Reconcile the combat design and authoring specs with what stage 6 actually built: mechanics carried on the encounter log, the `core:*` catalogue and the `dash` step, the measured node budget, roll pruning that keeps a roll consumed, the projection's degrade classes and upcastable slot costs, and the lease's single branded write-back into the legacy character combat state.
