---
---

`override` gains the direct-patch paths `position` (a placement: sets or clears the token's cell, recomputes adjacent/range, opens no opportunity-attack window, spends no movement — which is how a `log-only` table moves tokens) and `reveal.token` / `reveal.block` / `reveal.hp`.
