---
"d20-folio": minor
---

The sheet reads the engine world's standing occurrences — the first UI-read migration off the rollout bridge. Live active-key standings project into the one grant aggregation (an engine-cast Shield lifts effective AC by +5 with no legacy activation row; a buff active both ways dedupes by key identity), so reaction casts and self-owned mechanics-carrying while-active casts now dispatch through the deterministic runtime in and out of combat, with the reaction marker riding the exact legacy economy mirror. Engine-held concentration is coherent at every legacy break boundary: a failed entered-d20 save, a 0-HP break, a manual stop, or a legacy swap ends the world's concentration occurrence through the canonical end machinery in the same motion (standings cascade in the wave; the undo pairing reverses the exact journal action), and the commit mirror moves the legacy field on engine transitions only, so a legacy-held swap can never be resurrected.
