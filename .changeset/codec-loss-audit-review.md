---
---

Harden the codec-loss audit after review: parse a deep copy so in-place log normalization cannot mask a seam, anchor the tracker, log and unit seams to the exact paths their functions rewrite (a drop under the same prefix is a loss), type the combat-state key list against `CombatState` and project the nested `hp` / `deathSaves` shapes, and audit the whole library document.
