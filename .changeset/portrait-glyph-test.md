---
"d20-folio": patch
---

Regression test pinning the Portrait primitive's creature-type glyph fallback (drawn
over the deterministic tint when a glyphPath is supplied; the uploaded portrait still
wins — override-first).
