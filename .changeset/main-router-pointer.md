---
"d20-folio": patch
---

The `main` agent briefing becomes a short router: this checkout is production (fixes only); the product, the program and the knowledge base live on `v2` (`origin/v2:CLAUDE.md`, `origin/v2:docs/program/NEXT.md`), which binds Claude Code and Codex alike through the `AGENTS.md` symlink.
