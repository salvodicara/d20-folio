---
"d20-folio": patch
---

Feature-granted actions join the deterministic runtime: the transcriber compiles the tracker-payment family (Second Wind — spend one pool use, heal 1d10 plus the caller-resolved class-level bonus, THP rolls), legacy trackers seed the world's pools at first derivation, and the rollout bridge mirrors every spent use back onto the legacy tracker counter.
