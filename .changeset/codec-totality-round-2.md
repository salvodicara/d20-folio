---
---

Close the codec's remaining silent drops: `build.classes`, `build.customs.conditions`, `state.log` and the pact/manifested weapon maps are now total with typed paths, the quarantine primitives live in one shared pure module, and the totality claims in the docs are qualified with the enumerated seams that remain.
