---
"d20-folio": patch
---

Persisted engine worlds seed newly-tracked feature pools additively at read time — one-way from the legacy counters, never touching an existing pool — so every character migrates each tracker exactly once, with the first paying action.
