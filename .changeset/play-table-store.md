---
---

Add the play surface's table store: one listener on `campaigns/{id}/encounters/live`, a fold memoised on the log's content, `dispatch`/`undo` stamping the action envelope, and the DM's single-flight compaction.
