---
"d20-folio": patch
---

Docs: record the auto-narrated combat Phase 1 seam (sheet target capture → the
combat-state `recentActions` channel → the pure correlation layer → the reconciled
chronicle) in `docs/ARCHITECTURE.md` and `PROGRESS.md`.
