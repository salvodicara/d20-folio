---
"d20-folio": patch
---

fix(identity): the chrome reset's converged review — four real defects, and the guards that were sampling the one place the work was done. `.cmp-seal` survived the unframing with a border AND the exact banned two-line lip: it leads every row on eight of the ten codex tabs, so `/compendium?type=feature` rendered **49** framed boxes against a ceiling of 23 — the census only ever visited the default Spells tab, the one tab whose seals were already unframed. The census now samples each realm's VARIETY rather than its landing state (a `.cmp-seal` codex tab, three cockpit tabs, the campaign hub's running-encounter sub-view), which immediately surfaced a third framed level in the app chrome — the split combat pill's destination chip, whose divider is now a painted seam and whose cream lip is gone. The L3 geometry sweep omitted DISCLOSURE (`.is-open` / `[data-open]` / `[aria-expanded]`) and so could not see the phase's own flagship defect: re-inserting `.uc.is-open { border-left-width: 4px }` produced ZERO offenders. It is covered now, with the four disclosure-BODY recipes that legitimately resize revealed content named as exemptions with a stated reason. A new `.slot-cell.pact .sc-lvl` (0,3,0) silently out-ranked the light AA safety override two rules below it (0,2,0) at **2.91:1**, reachable by no rendered check we have because no mock character has pact slots — fixed to `--lapis-700` and pinned by a cascade-resolving unit guard that picks up any future `.slot-cell.<x> .sc-lvl` strike. And `button.vital[data-density="chip"]` ran its content into its own left border on both campaign-hub themes; the zero-left padding is now scoped to the read-only strike.
