---
"d20-folio": patch
---

Convergence sweep on the dialog unit: the dead EditingPill CSS family (~90
lines) dies with its component, the base modal sheds material declarations
the leather-plate twin always overrides, ModalScroll accepts an external ref
so the picker list composes the primitive instead of hand-rolling its recipe,
and stale comment references are repointed.
