---
"d20-folio": patch
---

test(e2e): a Chromium journey for the encounter picker + statblock disclosure (add a bestiary monster ×3 → pre-filled row with a blank initiative, the surviving Custom tab, the DM statblock modal, and the IT-name leg), plus two a11y/visual surface-manifest entries (the picker modal on its Bestiary tab + the monster statblock modal) so both enroll in the axe/i18n/mobile sweep.
