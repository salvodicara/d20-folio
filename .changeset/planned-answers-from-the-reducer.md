---
---

Plan every answer the reducer actually reads: a mark's rider dice (Hunter's Mark) now come from the reducer's own `riderAnswers`, and only a per-target `d20` expands into `${input}:${target}` keys, through the reducer's own `answerKeyFor`. A tile can no longer spend dice to learn a `missing-answer`.
