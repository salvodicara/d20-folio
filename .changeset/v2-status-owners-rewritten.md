---
"d20-folio": patch
---

Rewrite `docs/PROGRAM_STATUS.md` as the `v2` program ledger and `docs/TEST_PORTFOLIO.md` as the `v2` test portfolio (gate, lanes, golden-replay contract, deletion ledger); retitle the mechanics kernel section of the architecture map as legacy that dies at stage 6 and replace every "never roll" statement with the dice seam rule.
