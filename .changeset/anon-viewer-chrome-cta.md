---
"d20-folio": patch
---

feat(share): anonymous-viewer chrome + post-view conversion CTA on the public /view page (delivers the captured "post-view signup CTA" candidate). The header is the SAME bar in both auth states (owner gate) — identical brand (the exact same `<BrandMark variant="gilt" size="md">` element, rendered unconditionally), height, background, border, spacing. The ONLY difference logged-out is that the auth-gated right cluster (hub tabs + "Ask the Folio" palette + account menu) is replaced, IN THE SAME SLOT, by the sign-in affordances: `.topbar-anon` is a byte-for-byte mirror of the account cluster's box (`.topbar-user`), holding a quiet "Sign in" text link + a brass "Create your character" CTA — collapsing to a single "Get started" pill on mobile — all routing to the real /login Google auth entry (a route, so the eager shell carries no firebase/auth import). The brass CTA/pill wear the standard `.btn` vocabulary but are sized to the topbar's OWN control rhythm (the `.topbar-ask` scale — text-sm, not the tiny 10px in-card `.btn.sm`), so they read native to the bar rather than a chip bolted on; the signed-in bar is unchanged. After the sheet, an inline folio-panel conversion card ("Like this hero? Create your own on d20 Folio.") — the ONE allowed benefit moment (DESIGN.md §6) — with a quiet "Built with d20 Folio" wordmark loop, dismissed by scrolling, never a modal/sticky nag; shown only to anonymous viewers (a signed-in viewer sees just the sheet). Bilingual EN+IT, both themes, a11y (real links, no focus trap). The desktop-pair vs mobile-pill swap is driven by folio.css media rules, not Tailwind `sm:` utilities — `.btn` is unlayered and beats the layered utilities, so a `sm:hidden` on a `.btn` is a no-op (both would show at once). The CTA card lives in the lazy SharedCharacterView chunk; the eager entry ceiling stepped 62→63 KB for the topbar cluster (docs/ARCHITECTURE.md P3). Mutation-proven gating in the Topbar + SharedCharacterView unit suites.
