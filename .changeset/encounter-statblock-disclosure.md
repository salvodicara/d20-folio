---
"d20-folio": minor
---

feat(campaign): a DM-only "Statblock" disclosure on every picker-added encounter monster — one tap opens the full bestiary statblock (attacks, saves, immunities, legendary actions), titled with the combatant's own name over the canonical statblock name. Players never see it (it rides the existing DM-gated card body). The monster name is now renameable in place from the DM disclosure ("Goblin A" vs "Goblin B"), and a mid-fight reinforcement can receive its rolled initiative after turns have begun. A stale/unknown reference degrades to a quiet empty state, never an error.
