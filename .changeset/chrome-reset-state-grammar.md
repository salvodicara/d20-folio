---
"d20-folio": patch
---

feat(identity): the chrome reset, phase 5 — one state ladder. Five rungs in tokens (`index.css` §04b) — the hover/selected/disabled metals plus the hover and selected washes, with a per-theme pressed veil — consumed by cards, tiles, rows, tabs, chips, option cells, wizard entries and every button register, so fifty controls share one grammar instead of fifty bespoke hovers. Every geometry-changing state is deleted: state changes light and colour only, and a sweep in `chrome-system.guard.test.ts` fails any state rule that declares a geometry property.
