---
"d20-folio": patch
---

feat(identity): the chrome reset, phase 5 — one state ladder. Five rungs in tokens (`index.css` §04b) — the hover/selected/disabled metals plus the hover and selected washes, with a per-theme pressed veil — consumed by cards, tiles, rows, tabs, chips, option cells, wizard entries and every button register, so fifty controls share one grammar instead of fifty bespoke hovers. Every geometry-changing POINTER, FOCUS and SELECTION state is deleted: those states change light and colour only, and a sweep in `chrome-system.guard.test.ts` fails any state rule that declares a geometry property. DISCLOSURE states are the stated exception and still resize — not the disclosure's frame, which the sweep holds frozen too, but the content it REVEALS (the search field unfurling from its lens, the wizard entry's open row, the session body, the action row's detail panel), each named as an exemption with its reason.
