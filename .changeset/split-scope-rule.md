---
"d20-folio": patch
---

docs(rules): add golden rule D11 — the public/pack split is a licensing boundary, never a scope boundary, so every change is designed against the full game and pack-side twins update in the same motion.
