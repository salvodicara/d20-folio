---
"d20-folio": patch
---

The last surfaces join the grammar scope: the login splash, both error
boundaries (region + fullscreen crash), and the folio loader — closing the
"no surface excluded" sweep.
