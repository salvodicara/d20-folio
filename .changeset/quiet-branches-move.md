---
"d20-folio": patch
---

Keep commits and branch checkpoints immediate: pre-commit checks only staged metadata/files, while the complete repository gate runs once for the finished integration into `main`.
