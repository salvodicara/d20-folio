---
"d20-folio": patch
---

E2E gate integrity (test audit, wave 1): the Signet mobile suite and the touch-gate coarse-pointer halves actually run again (the mobile project's testMatch had silently orphaned every mobile-gated spec), the redundant navigate-only pass of the 344-test visual matrix left the ambient gate (its coverage is fully owned by the a11y + i18n sweeps), and every fixed-timeout race in the palette/level-up/rest specs became a readiness or stable-box poll.
