---
"d20-folio": patch
---

E2E gate integrity (test audit, wave 1): the Signet mobile suite and the touch-gate coarse-pointer halves actually run again (the mobile project's testMatch had silently orphaned every mobile-gated spec — and the two ungated width sweeps in mobile-layout now self-skip under touch), the redundant navigate-only pass of the 344-test visual matrix left the ambient gate entirely (skipped before any browser context is built), every fixed-timeout race in the palette/level-up/rest specs became a readiness or stable-box poll (one shared `settledBox` helper in ready.ts), and the merged raw-palette guard carries an anti-vacuity pin.
