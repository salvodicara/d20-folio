---
"d20-folio": patch
---

Fix the coordinator's concentration-replacement livelock: an end wave's exactly-once source-ending delivery is now keyed by the wave's candidate set (the identity that survives audience-driven rebasing) instead of the world basis, and mid-action wave finalization grants the live frame stack's one-ahead provenance permits, so casting a concentration spell while the engine holds one converges as ONE bounded causal action — old occurrence, root and standings swept, the new cast held, exact undo restoring the old spell — instead of burning the whole work budget.
