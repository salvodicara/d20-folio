---
"d20-folio": patch
---

Fix DESIGN.md: state the mounted shell's real `primaryDestinations` order (campaign, table, characters, library) instead of a false claim it matches the four-scopes reading order, count `identity.css`'s six locally scoped tokens instead of five, and widen the frontmatter's token-pruning rule to cover tokens consumed outside `src/index.css`.
