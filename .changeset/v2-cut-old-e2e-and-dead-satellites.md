---
"d20-folio": patch
---

Cut the old end-to-end suites on `v2` (60 specs, the pixel harnesses, the perf probe and the service-worker projects; the accessibility sweep and the owner's screenshot lane stay), delete the dead engine satellites (`combat-io.ts`, `mechanics-trigger.ts`) and the superseded Wayfinder, K1 and P2-prototype plans and status records; freeze the mechanics kernel behind an importer guard until stage 6.
