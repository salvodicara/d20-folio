---
---

Pin the stage-3 reducer behaviour the branch review found unobserved: a roll answered by a
`log-only` intent is still consumed once (ADR-0010 × ADR-0011), a caster inside their own blast is
a target while an eligibility predicate can exclude one, cube/cone/line membership is proven by a
probe pair that differs only in the property each shape governs, a cone without its aim is
rejected, the monster adapter degrades a use-time damage choice and an `onSuccess: "special"` save
to `manual-table`, and the prototype catalogue's hand-copied Ogre is proven equal to the real SRD
corpus entry.
