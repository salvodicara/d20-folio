---
"d20-folio": patch
---

Drop the vendored `impeccable` skill copy (`.claude/skills/impeccable/`, `.agents/skills/impeccable`) and its `.prettierignore` entry; the global skill layer at `~/.agents/skills/impeccable` 4.1.1 is canonical for both harnesses.
