---
"d20-folio": minor
---

The hero frames gain their BINDING CORNERS: the silhouette of a human-engraved
frame-corner spandrel (Openclipart #282416, CC0 — vertex knot removed by vector surgery)
mounts as a CSS mask on `.folio-panel.gilt-frame`, painted with the border's own standard
ink mix so fitting and frame share one colour by construction in both themes; the tile
origin sits on the outer rule's outer edge and the fitting lies pixel-coincident on the
host's 1px border (row-verified), with the host corner squared beneath it as geometric
necessity. The masthead vitals keep their carved v0.22.0 tiles (owner ruling, recorded in
DESIGN.md §16.7).
