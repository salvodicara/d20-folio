---
"d20-folio": patch
---

Turn the campaign hub into a compact task workspace: Live keeps encounter and a durable autosaving
session recap side by side, Journal/Resources/DM become real state-preserving local tabs, and desktop
encounter identity plus vitals fit one scannable tactical row without changing live combat state.
Opening a different workspace also returns the viewport to that task's readable beginning while its
mounted draft and disclosure state remain intact. The DM workspace keeps an explicit localized label
in both the interface and its visual-regression contract.
