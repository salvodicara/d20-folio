---
"d20-folio": patch
---

The creation and level-up wizards join the grammar scope: WizardFrame carries
`wb` so every shared recipe resolves inside the wizards exactly as on every
other surface. Their ratified ritual register (scene, orbs, plaques) is
untouched — the scope is inert where the wiz vocabulary owns the look.
