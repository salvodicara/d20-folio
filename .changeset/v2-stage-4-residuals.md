---
---

Record the stage-4 re-review's four residual observations on the rolls-pruning gap, the checkpoint liveness cliff, the import guard's regex scope, and the sortedness of `ReactionWindow.eligible`.
