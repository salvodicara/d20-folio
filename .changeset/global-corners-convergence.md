---
"d20-folio": patch
---

Convergence sweep on the global binding corners + book tabs: the retired reliquary's
comment essays and dead declarations leave `index.css`/`folio.css`, the legacy tabstrip
visual rules (which silently beat the twinned dark hover) delete so the linguette twinning
is actually true, TabStrip's dead utility classes drop, DESIGN.md's fitting entry tells the
mask/inset/square-corner truth, and a vacuous guard regex goes.
