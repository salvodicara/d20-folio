---
"d20-folio": patch
---

Roadmap capture (docs-only): record the owner's deterministic combat-log idea (2026-07-31) as a
named CANDIDATE rung on the DDB-parity attack order — an auto-generated, structured MECHANICAL log
per encounter (HP math, who-hit-whom, dying, conditions, round advancement), event-sourced from the
actions already flowing through the shipped encounter/initiative tracker and appended to the
campaign at encounter-close. No AI, no dice — it complements the DM's hand-written story notes
rather than generating prose. Captured with the feasibility framing (on-brand without AI, a
DDB-parity-PLUS DM feature) and recorded as the budget-safe reframing of the parked "Table feed"
(the encounter boundary replaces per-action campaign writes). Includes the settled attribution
design (owner + orchestrator 2026-07-31): the damage attacker is captured by an optional one-tap
attribution pre-selected to the current-turn combatant, never inferred — the engine records facts,
never guesses, so an unattributed hit reads "the Goblin takes 8 damage", never a fabricated
attacker. Priority stays the owner's call.
