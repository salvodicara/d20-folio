---
"d20-folio": patch
---

Add explicit catalogue snapshots and typed acquisition pools while preserving Library versions. Freeze initial item source closures in one loadout, compose them through the existing item repository, and compare full grouped state when editing an acquired copy.
