---
"d20-folio": minor
---

Auto-narrated combat, Phase 2 (multi-target prose): the combat chronicle can now render ONE line for a multi-target action — "{attacker} hits A (x), B (y) and C (z)" — summarizing the real per-target damage the DM applied, in EN and IT (a full whiff reads "{attacker} misses A, B and C"). Adds the `attack-multi` chronicle event kind and its bilingual presenter templates with a natural-language enumeration join (reusing the shared `common.and` conjunction), reading correctly for 2, 3, or N targets.
