---
"d20-folio": patch
---

feat(identity): the chrome reset, phase 6 — the MARK returns. Two members of one vector family, both drawn as the line's own local form rather than as a second rail: a corner terminal (seven hair-thin tapering rays anchored on the corner arc, radiating inward, contributing no run line) and a run cartouche (pointed leaves weaving over and under the rail onto a descending chevron at the exact midpoint). Dimensional metal — the geometry mirrored per corner and toned after the mirror in screen space, so the bevel's light stays top-left on all four — gold in both themes. Mounted on exactly ONE surface per route, the screen's identity plate; measured at 2.40× the rule's ink over 19–24% of the run, against the reference's 2.0–2.5× and 34%.
