---
---

Enumerate the character codec's documented one-way read seams (`CODEC_READ_SEAMS`) and the combat-state keys the reader sheds (`SHED_COMBAT_STATE_KEYS`); the codec-loss audit classifies a round-trip change on one of them as `conformed` and everything else as loss.
