---
---

Roll one damage die per swing: the dispatch plan skips an input whose every reader is ruled out by a `when` the answers already decide, so a Versatile weapon no longer appends an unread second damage roll to the shared log.
