---
"d20-folio": patch
---

Make the five `.claude/skills/firebase-*` directories git-tracked symlinks to the canonical `.agents/skills/firebase-*` copies, so there is one source instead of a byte-identical duplicate per harness.
