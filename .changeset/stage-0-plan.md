---
---

Add the stage 0 (data safety gate on `v2`) implementation plan: merge the P1 data safety from `main`, prove zero codec loss over the six team fixtures and a production export with a reproducible audit, record the gate.
