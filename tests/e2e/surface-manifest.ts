/**
 * Compatibility exports for legacy route guards and Playwright traversals.
 * New surface authoring belongs in `surface-census/index.ts` or a slice-owned
 * `surface-census/<family>.ts` fragment registered by that index.
 */
export { SURFACE_ROUTES, type SurfaceRoute } from "./surface-census";
